#!/bin/sh
set -e
if [ "$1" = "configure" ]; then
    systemctl daemon-reload || true
    systemctl enable pardus-usb-syncguard.service || true
    systemctl start pardus-usb-syncguard.service || true
fi
#DEBHELPER#
exit 0
