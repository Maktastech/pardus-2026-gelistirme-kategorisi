#!/bin/sh
set -e
if [ "$1" = "configure" ]; then
    systemctl daemon-reload || true
    systemctl enable pardus-ram-guard.service || true
    systemctl start pardus-ram-guard.service || true
fi
#DEBHELPER#
exit 0
